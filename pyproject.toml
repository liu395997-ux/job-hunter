[project]
name = "jobhunter"
version = "0.1.0"
description = "自动化求职工具：招聘数据采集、清洗、分析与个性化推荐"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
