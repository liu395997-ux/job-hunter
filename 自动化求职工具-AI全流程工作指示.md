# 自动化求职工具 · AI 全流程工作指示书

> 适用课程：短学期 Python 数据分析与可视化（32 学时，09/01 – 09/06，5 人组队）
> 项目定位：往届题目 3「招聘网站职位信息可视化分析与个性化推荐」的自动化升级版
> 本文档用途：既是开发计划，也是交给 AI 编程助手（Trae / Qoder / DSH / Opencode 等）的分阶段工作指令。
> 使用方法：不要一次性把全文丢给 AI，按阶段 0–8 逐段粘贴，每阶段完成后对照「验收标准」检查，再进入下一阶段。

---

## 0. 一句话定位

面向学生求职者的自动化求职工具：自动爬取招聘网站的职位信息 → 清洗入库 → 分析薪资 / 技能 / 企业画像 → 基于简历做个性化推荐 → 自动生成求职报告，形成「采集—分析—推荐—报告」闭环，并用 Streamlit Web 界面可视化全流程。

---

## 1. 课程硬性要求 → 项目任务映射

| 课程要求（来自开学.txt） | 本项目落实方式 |
| --- | --- |
| 一般应是一个 Web 项目 | Streamlit 多页面 Web 应用 |
| 至少包含三个不同类型的算法 | ① 内容推荐（TF-IDF + 余弦相似度）② 薪资预测（随机森林回归）③ 岗位聚类（KMeans）④ 技能需求图谱（词频 + 共现网络，亮点） |
| 数据爬取、清洗、转换及管理 | requests / requests-html 爬虫 + pandas 清洗 + SQLite 存储 |
| 前端可视化算法过程与结果 | 每个算法单独展示页，含过程、评估指标、与基线对比 |
| 代码规范、模块化设计、有重构案例 | 分层目录、ruff 规范、记录一次重构案例 |
| 特别考查 AI 工具使用（提示词、skill、plugin、workflow） | 全程记录 docs/ai_usage.md，每人至少演示一次 AI 用法 |
| 团队协作记录 | git 提交记录 + docs/meetings.md 会议纪要 |
| 材料：立项报告 / 中期报告 / 期末报告 / 答辩 PPT / 代码.zip | 见第 10 节产出物清单 |

---

## 2. 六天日程规划 + 里程碑

| 时间 | 主要任务 | 交付物 | 里程碑 |
| --- | --- | --- | --- |
| 开课前（08-31） | 组队、环境准备、LLM API 账号、数据源摸底 | 立项材料框架 | — |
| Day1（09-01） | 立项答辩，按老师意见整改 | 立项报告（初稿） | M1 立项通过 |
| Day2（09-02） | 开发环境 + 产品原型 + 概念验证 | 可运行原型：爬通一个页面 + 一张图 + 一个算法雏形 | M1 概念验证 |
| Day3（09-03） | 核心开发：爬虫、清洗、存储、算法 1–2、看板页 | 中期可演示版本 | — |
| Day4（09-04） | 中期检查：上机演示 + 中期报告（每人 1 份）；晚上 Python 在线测试 | 中期报告 ×5 | M2 中期检查 |
| Day5（09-05） | 推荐算法、自动化功能、前端完善、测试 | V1.0 候选版 | — |
| Day6（09-06） | 验收答辩：演示视频 + PPT + 现场演示 | 验收通过 | M3 项目验收 |
| Day7（09-07） | 期末报告、答辩 PPT、代码打包 | `[学号]-[组长].zip` | 提交 |

版本规划：V0.1 概念验证 → V0.5 中期可演示 → V1.0 验收版。
三个里程碑：概念验证（Day2）、中期检查（Day4）、项目验收（Day6），与课程检查点一一对应。

---

## 3. 技术方案

### 3.1 技术栈

- 语言 / 环境：Python 3.11+，包管理用 uv 或 miniconda；Jupyter 用于探索实验，Streamlit 用于产品。
- 核心依赖：requests、requests-html、pandas、scikit-learn、matplotlib、jieba、wordcloud、networkx、APScheduler、sqlite3（内置）。
- 数据源：智联招聘 / 51job / BOSS 直聘（任选 1–2 个），关键词选「数据分析 / Python 开发 / 算法」等大数据相关职位。
- 兜底数据：`data/sample_jobs.csv`（≥300 条，字段与真实爬取一致），防止反爬或断网导致演示失败。

### 3.2 系统架构（五层）

```
采集层（爬虫调度 / 解析 / 反爬）
    ↓
数据层（清洗 / 标准化 / SQLite 存储）
    ↓
算法层（推荐 / 薪资预测 / 聚类 / 技能图谱）
    ↓
展示层（Streamlit 多页面可视化）
    ↓
自动化层（定时爬取 / 自动报告 / 匹配提醒）
```

### 3.3 项目目录结构（模块化，答辩重点）

```
jobhunter/
├── config/            # 站点、关键词、爬取频率、用户画像配置
├── spider/            # 采集：调度器、解析器、反爬策略
├── cleaner/           # 清洗：薪资解析、去重、标准化
├── storage/           # SQLite 建表读写、CSV 导入导出
├── analysis/          # 算法：recommend / salary_predict / cluster / skill_graph
├── ui/                # Streamlit 入口与 pages/
├── automation/        # 定时任务、报告生成
├── data/              # raw/ cleaned/ sample/
├── tests/             # pytest 单元测试
├── docs/              # 立项报告、中期/期末报告、踩坑记录、AI 使用记录
└── README.md          # 安装与使用说明
```

---

## 4. 算法清单（≥3 个，全部带评估）

1. 个性化推荐（内容过滤）：简历画像（技能 / 学历 / 城市 / 薪资期望）与职位描述做 TF-IDF 文本相似度，叠加规则硬过滤（城市、学历、经验）→ 输出 Top-N 岗位 + 匹配分。评估：Top-N 人工校验命中率。
2. 薪资区间预测（回归）：特征 = 城市、学历、经验、公司规模、技能数量、岗位类别；随机森林回归 → 预测薪资区间 + 特征重要性。评估：MAE、R²。
3. 岗位聚类（KMeans）：对职位描述 / 技能向量聚类，输出岗位族画像与分布。评估：轮廓系数选 k。
4. 技能需求图谱（创新亮点）：技能词频 Top-N 词云 + 技能共现网络，展示热门技能与组合关系。

每个算法必须能口头讲清：为什么选这个算法、数据怎么划分、指标怎么算、结果说明了什么（答辩必问）。

---

## 5. 自动化特色（呼应“自动化求职工具”的题目）

- 定时爬取：APScheduler 每日 / 每 6 小时自动增量爬取，频率可在 config 配置。
- 自动求职日报：每天生成 Markdown/HTML 日报（新增岗位、薪资变化、热门技能、匹配提醒）。
- 简历匹配提醒：按用户画像每日自动计算匹配度，输出「今日最值得投递的 Top 10 岗位 + 匹配理由」。
- 投递材料包生成：为每个匹配岗位自动生成投递摘要（匹配亮点、技能契合点），供求职者人工投递。
- 合规边界：不自动代替用户投递简历，只自动准备材料（课程项目 + 道德考虑）。

---

## 6. 团队分工（5 人，可按实际微调）

| 角色 | 负责模块 | 答辩展示点 |
| --- | --- | --- |
| 组长 | 架构设计、进度管理、联调、答辩统筹 | 总体演示、架构图、协作记录、项目管理 |
| 成员 A（爬虫） | 采集层 | 反爬对策、自研调度（DFS/BFS 切换）、解析规则 |
| 成员 B（数据） | 清洗 + 存储 | 清洗前后对比、数据字典、SQLite 表设计 |
| 成员 C（算法） | 推荐 + 薪资预测 | 模型过程、评估指标、与基线对比 |
| 成员 D（算法 + 前端） | 聚类 + 技能图谱 + Streamlit 页面 | 图表选型理由、交互控件设计、可视化结果 |

说明：若团队只有 4 人，把「自动化功能」并入成员 D 或成员 A；若 5 人且都想有算法，可把「自动化报告」拆给成员 E。

---

## 7. 分阶段 AI 工作指示（核心）

> 给 AI 的每段指令都包含：目标 → 任务 → 产出 → 验收标准。粘贴给 AI 后，要求 AI 先复述计划再动手。

### 阶段 0：环境与项目初始化（开课前 / Day2 上午）

**指令**：用 uv（或 miniconda）创建 Python 3.11 环境并安装全部依赖；按 3.3 目录结构初始化项目；初始化 git 与 .gitignore；编写 README.md 框架。

**验收**：`streamlit --version`、`python -c "import pandas, sklearn, jieba"` 均通过；目录结构完整。

### 阶段 1：立项报告（Day1）

**指令**：按课程立项指南生成立项报告，包含：
- 摘要 ≤200 字，回答为什么做 / 做什么 / 怎么做；
- 调研 ≥3 个同类产品（如 BOSS 直聘职位推荐、智联招聘推荐、LinkedIn Job Match、拉勾推荐），从功能、技术、数据三个维度做对比表并评述；
- 需求列表：6–8 条用户故事（例如“作为应届生，我想输入我的技能与期望薪资，以便看到匹配度最高的岗位”）；
- 技术方案：架构、平台、语言、框架、库、环境、项目管理工具；
- 团队分工与任务列表；
- 开发计划：V0.1 → V1.0 版本计划 + 3 个里程碑（概念验证 / 中期检查 / 项目验收）；
- 可行性分析：技术可行性、团队可行性（内部纪律、会议、代码审查、git 管理）。

**产出**：`docs/立项报告.md` + 立项答辩 PPT 大纲（每人 1 分钟自我介绍要点）。

**验收**：覆盖立项指南全部小节；摘要 ≤200 字；答辩演示可在 10 分钟内讲完。

### 阶段 2：数据采集（Day2–Day3）

**指令**：
- 选定 1–2 个招聘站点，写可配置爬虫（站点、关键词、城市、翻页数、频率全部放 config）；
- 自研调度策略：支持深度优先 / 广度优先切换；支持限速、UA 池、重试、断点续爬；
- 提取字段：职位名称、薪资文本、城市、学历、经验、公司、公司规模、公司性质、技能要求、职位描述、发布时间、链接；
- 原始数据存 `data/raw/`，结构化结果存 CSV；
- 生成兜底样例数据 `data/sample_jobs.csv`（≥300 条，字段分布模拟真实数据）；
- 写 `docs/爬虫调研与反爬笔记.md`：目标网页结构、反爬特征、应对方法。

**验收**：真实环境稳定爬取 ≥200 条有效数据；断网时用样例数据也能跑通全流程。

### 阶段 3：清洗与存储（Day3）

**指令**：
- 薪资解析：“15-25K·14薪” → min/max/avg（统一为 K/月），缺省处理；
- 去重（职位 + 公司 + 城市 + 薪资）、缺失值处理、学历 / 经验 / 公司规模标准化映射；
- 技能提取：jieba 分词 + 预置技能词典（Python / SQL / Spark / 机器学习…）；
- SQLite 建表 jobs / skills / companies；CSV 导入导出；输出清洗前后对比统计报告；
- 生成 `docs/data_dictionary.md` 数据字典。

**验收**：清洗脚本可重复运行；报告展示清洗前后行数、去重数、缺失率。

### 阶段 4：算法实现（Day3–Day5，先通后优）

**指令**：
- 算法 1 推荐：简历画像 + TF-IDF + 余弦相似度 → Top-N 与匹配分，叠加城市 / 学历 / 经验硬过滤；
- 算法 2 薪资预测：划分训练 / 验证集，随机森林回归，输出 MAE、R²、特征重要性图；
- 算法 3 聚类：KMeans + 轮廓系数选 k，输出每簇画像；
- 算法 4 技能图谱：词云 + networkx 共现网络；
- 每个算法独立模块 + 一个 Streamlit 展示页（过程、结果、评估、与基线对比）；
- 关键代码写清「为什么选这个算法 / 图表」的注释。

**验收**：4 个算法全部跑通并有可视化；每个算法能口头讲清原理与指标。

### 阶段 5：Web 前端（Day4–Day5）

**指令**：用 Streamlit 实现多页面应用：
1. 市场总览：薪资分布、岗位数量、城市 / 学历分布（柱状图 / 饼图）；
2. 薪资分析：分城市、学历、经验的薪资箱线图 / 折线图；
3. 技能图谱：词云 + 共现网络；
4. 我的求职匹配：表单输入城市 / 学历 / 技能 / 期望薪资 → 推荐 Top-N 岗位卡片 + 匹配雷达图；
5. 自动化报告：展示定时任务日志与最近生成的日报。

交互控件用下拉框、滑杆、多选、按钮；每个图表写明选型理由（为什么用箱线而不是折线等）。

**验收**：`streamlit run app.py` 一键启动，按演示脚本 5 分钟内走通全流程。

### 阶段 6：自动化功能（Day5）

**指令**：
- APScheduler 定时增量爬取 + 自动清洗入库；
- 自动生成日报 / 周报（新增岗位 Top 榜、薪资变动、推荐岗位清单 + 理由）；
- 用户画像配置化（config/user_profile.yaml），每日自动匹配；
- 一键导出投递材料包（每岗位的匹配亮点摘要）。

**验收**：展示定时任务日志 + 最新报告；能解释调度配置与增量逻辑。

### 阶段 7：测试、规范与文档（Day5–Day6）

**指令**：
- 为关键函数写 pytest 单测：薪资解析、去重、相似度排序、清洗 pipeline；
- 用 ruff 做代码规范检查；做一次代码重构并记录（例如把爬虫解析从大函数拆成解析器类，记录前后对比）；
- 完善 README：安装、配置、运行、目录说明；
- 写 `docs/踩坑记录.md`（≥5 条真实踩坑：编码问题、反爬、薪资格式、站点改版等，每条含现象 / 尝试 / 解决 / 原理）。

**验收**：`pytest` 全部通过；`ruff check` 通过；按 README 可复现。

### 阶段 8：答辩与期末材料（Day6–Day7）

**指令**：
- 期末报告模板每人一份：个人学习与开发过程回顾、总结与展望、思政心得，结合本人负责模块；
- 答辩 PPT：组长总体部分（功能演示、技术架构、项目管理与协作）+ 每人演示部分；嵌入 5 段演示视频（mp4/webm，分辨率 800×600 左右，每段 ≤2 分钟）；
- 演示脚本：每人 2 分钟讲什么、被抽到代码时怎么答（列出每模块关键代码行）；
- 材料打包：`[学号]-[组长].zip` = 各人 `[学号]-[姓名]-期末报告.docx` + `答辩.ppt` + `代码.zip`（代码含 README）；
- 整理 `docs/ai_usage.md`：每人用过的 AI 提示词示例、skill / plugin / workflow、效果截图。

**验收**：材料齐全、命名合规、视频可播放、每人能独立演示自己模块。

---

## 8. 与 AI 协作的通用规则

- 一次只给一个阶段的指令，完成并对照验收标准后再进下一阶段；
- 要求 AI 每次改动后跑测试并同步更新 README；
- AI 生成的关键代码必须逐行理解后再提交（答辩随机抽题，必须讲得清）；
- 爬不到数据时立即切兜底样例数据，不阻塞演示；
- 重要对话的提示词与输出摘要记入 docs/ai_usage.md（这是考核项）；
- 优先原则：先打通「爬 → 存 → 一个算法 → 一个页面」的最小链路，再扩展和美化。

---

## 9. 风险与应对

| 风险 | 应对 |
| --- | --- |
| 反爬封禁 | 限速、UA 池、换源、兜底样例数据 |
| 招聘网站改版导致解析失效 | 解析器独立成模块、selector 配置化，快速修复 |
| 真实数据量不足 | sample_jobs.csv 兜底 + 合理扩充关键词和城市 |
| 答辩抽到细节问题 | 代码注释、每人深度掌握自己模块、准备踩坑记录 |
| 时间不足 | 链路优先、美化后置；每天收工时功能可运行 |
| AI 生成的代码不理解 | 强制 AI 逐段讲解并加注释，理解后才合入 |

---

## 10. 关键产出物清单（对照课程提交要求）

| 提交物 | 时间 | 格式 |
| --- | --- | --- |
| 立项报告 | Day1 | md（后续可转 docx） |
| 产品原型（可运行可演示） | Day2 | 现场运行 |
| 中期报告（每人一份） | Day4 | 每人一份 |
| 期末报告 | Day7 | `[学号]-[姓名]-期末报告.docx` |
| 答辩 PPT | Day7 | `答辩.ppt`（含 5 段演示视频） |
| 代码包 | Day7 | `代码.zip`（含 README） |
| 汇总包 | Day7 | `[学号]-[组长].zip` |

---

## 11. 第一天的行动清单（立项前）

1. 确认组队 5 人、定组长，注册 LLM API 账号并装好 AI 编程助手；
2. 用 uv / miniconda 搭好 Python 环境（阶段 0）；
3. 用 AI 生成立项报告初稿（阶段 1），打印核对；
4. 准备好答辩话术：组长 4 分钟总体介绍 + 每人 1 分钟自我介绍与分工；
5. 数据源摸底：试跑一次爬虫或确认兜底数据可用，答辩时能说“已验证数据可得性”。
